"""Demo bot package.

Note: this module intentionally does NOT import :mod:`examples.bot.app` (which
pulls in aiogram), so that ``from examples.bot import text`` stays dependency
free and unit tests can run without the bot framework installed.
"""
